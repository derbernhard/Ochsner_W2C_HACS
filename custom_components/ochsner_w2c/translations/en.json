{
  "config": {
    "step": {
      "user": {
        "title": "Connect Ochsner W2C directly",
        "description": "Direct SOAP connection to the W2C. MQTT publishing is optional and uses the MQTT integration configured in Home Assistant.",
        "data": {
          "w2c_host": "W2C IP address",
          "username": "Username",
          "password": "Password",
          "scan_interval": "Polling interval (seconds)",
          "mqtt_enabled": "Also publish values via Home Assistant MQTT",
          "mqtt_base_topic": "MQTT base topic"
        }
      }
    },
    "error": {
      "cannot_connect": "Direct connection to the W2C failed."
    },
    "abort": {
      "already_configured": "This W2C device is already configured."
    }
  }
}
